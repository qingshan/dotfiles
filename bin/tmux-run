#!/usr/bin/env bash

tmux select-pane -t left
tmux if-shell -F '#{e|==:#{window_panes},1}' 'split-window -d -h -l 80 -c "#{pane_current_path}"' 'respawn-pane -k -t right'
tmux run-shell 'sleep 0.2'
tmux send-keys -t right "$*" Enter
